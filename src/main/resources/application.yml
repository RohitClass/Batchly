spring:
  datasource:
    url: jdbc:mysql://192.168.1.36:3306/batchly_db?useSSL=false&serverTimezone=UTC
    username: batchly
    password: "MnXmy*C81wJ.5kI8"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    serialization:
      INDENT_OUTPUT: true

server:
  port: 8080

jwt:
  secret: your-secret-key
  expiration: 86400000 # 1 day
